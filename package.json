{
  "name": "this-icon",
  "version": "0.1.0",
  "description": "An ever expanding icon library, designed for software interfaces",
  "main": "dist",
  "scripts": {
    "build": "npm run clean && npm run build:svg && npm run build:sprite && npm run build:js",
    "build:svg": "mkdir -p dist/svg && cp -r icons/* dist/svg/",
    "build:sprite": "echo 'Sprite sheet generation - to be implemented'",
    "build:js": "node scripts/generate-icon-map.js",
    "dev": "npm run build && echo 'Watching for changes... (to be implemented with nodemon)'",
    "lint": "echo 'SVG validation - to be implemented'",
    "clean": "rm -rf dist/"
  },
  "keywords": [
    "icon",
    "svg",
    "icon-library",
    "design-system",
    "ui",
    "interface"
  ],
  "author": "Chukwudi Ogbonnaya",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/signorchuksy/this-icon.git"
  }
}
